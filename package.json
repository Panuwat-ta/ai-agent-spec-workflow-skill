{
  "name": "ai-agent-spec-workflow-skill",
  "version": "1.1.0",
  "description": "Spec-Driven Development CLI — Plan first, then build.",
  "bin": {
    "agent": "./dist/cli.js"
  },
  "files": [
    "dist",
    "examples"
  ],
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "prepack": "npm run build"
  },
  "keywords": [
    "ai",
    "agent",
    "spec",
    "workflow",
    "cli"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "^26.1.1",
    "jest": "^30.4.2",
    "ts-jest": "^29.4.12",
    "typescript": "^5.9.3"
  }
}
